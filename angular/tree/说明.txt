首先，我在参考 http://www.ngnice.com/showcase/#/tree/checkbox 实现树的，一些复选框的选择效果，使用静态数据，实现了链接里的效果

第二，业务：给不同的角色配置不同的分组菜单，
			注意：菜单是在分组下的，菜单和分组是多对多关系（请看json数据）

第三，方案一, 当我只是请求“所有的分组菜单，而不分该角色下的分组菜单”，即请求 'Json/AllGroupMenus.json' ，获取的数据，页面的选择情况和上面写的效果是一样的，
但实际情况是，我还要请求“该角色下的分组菜单”，即请求 'Json/groupMenusRole.json'.
通过某个方法transGroupMenus，对比两个请求数据，给不同的分支checked=true属性，但是问题是方法transGroupMenus总是先执行，然后再从后台获取数据，解决方法可能需要用的angular中的$q服务，但是没有实例，不会写。。。。

第四，方案二：在后台对比“所有的分组菜单”、“该角色下的分组菜单”，即请求 'Json/groupMenusChecked.json'
	 在前台直接获取已经添加checked=true的数据
	 问题：后台的数据没有问题，关键是传到前台后，只要分组添加了checked=true，其所有的子级都添加checked=true，而且，之前所做的“在某个分组下没有选择其所有菜单时，...等等效果没有了”
	 猜测可能是“冒泡机制”或者自己写的“监听机制”有问题，需要改进.....


最后，各位帮帮忙，有时间给看一下啊，求大神指点O(∩_∩)O	 